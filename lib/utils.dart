import 'package:flutter/material.dart';

/// 生成EdgeInsets
EdgeInsets setEdgeInsets (double top, double right, double bottom, double left) {
  return EdgeInsets.only(
    top: top, right: right, bottom: bottom, left: left
  );
}